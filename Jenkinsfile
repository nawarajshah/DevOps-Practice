pipeline {
    agent any

    tools {
        // Specify the correct Maven installation name
        maven 'Maven'
    }

    stages {
        // Your stages go here
    }

    post {
        // Your post-build actions go here
    }
}
